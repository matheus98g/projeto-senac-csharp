# 1️⃣ Use Node.js LTS image
FROM node:20

# 2️⃣ Set working directory inside container
WORKDIR /app

# 3️⃣ Install dependencies separately (better caching)
COPY package*.json ./
RUN npm install

# 4️⃣ Copy the rest of the app
COPY . .

# 5️⃣ Expose the dev server port
EXPOSE 5173

# 6️⃣ Run Vite in dev mode and listen on all network interfaces
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
